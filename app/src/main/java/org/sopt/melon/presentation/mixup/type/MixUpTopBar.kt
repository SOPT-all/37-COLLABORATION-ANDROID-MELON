package org.sopt.melon.presentation.mixup.type

import org.sopt.melon.R

enum class MixUpTopBar(
    val displayName: Int,
) {
    PLAYLIST(R.string.playlist),
    MUSIC_SHELF(R.string.music_shelf),
    MIXUP(R.string.mixup),
}
